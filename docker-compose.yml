name: beb-raid

services:
  beb-raid:
    build: .
    container_name: beb-raid
    env_file: .env
    mem_limit: 1g
    shm_size: 256m
    volumes:
      - ./data:/app/data
    restart: unless-stopped
